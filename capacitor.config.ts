import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'healthapplication',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
